/**
 * Module with shared componets for all application.
 * @module base.module
 */

// Dependencies

// Feature modules

/**
 * Module name
 */
export const baseModule = 'base'

// Define the module
angular
  .module(baseModule, [])