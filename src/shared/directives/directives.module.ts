/**
 * Group shared directives for all app.
 * @module directives
 */

// Dependencies

// Directives
// import { closeDdDirective, closeDdDirectiveName } from './close-dd.directive'

/**
 * Module name
 */
export const directivesModule = 'directives'

// Define module
angular
  .module(directivesModule, [])
  // .directive(closeDdDirectiveName, closeDdDirective)